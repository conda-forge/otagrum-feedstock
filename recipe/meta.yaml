{% set version = "0.2" %}

package:
  name: otagrum
  version: {{ version }}

source:
  #fn: otagrum-{{ version }}.tar.gz
  #url: https://github.com/openturns/otagrum/archive/v{{ version }}.tar.gz
  #sha256: 853eff2e7ff59a03424c9b03bf9d508c59ccde668cc056a7da312170475bc30c

  # Use git waiting for the 0.2 release
  git_url: https://github.com/openturns/otagrum.git
  git_rev: 90e2f4f12ec50e0f4f4d00d50fc50e635bfcba99

build:
  number: 4
  skip: true  # [win]

requirements:
  build:
    - cmake  # [not win]
    - {{ compiler('c') }}  # [not win]
    - {{ compiler('cxx') }}  # [not win]
  host:
    - python
    - swig  # [not win]
    - openturns  # [not win]
    - pyagrum  # [not win]
  run:
    - python

test:
  imports:
    - otagrum

about:
  home: http://openturns.org/
  license: LGPL3
  license_file: COPYING
  summary: OT/aGrUM module

extra:
  recipe-maintainers:
    - jschueller
