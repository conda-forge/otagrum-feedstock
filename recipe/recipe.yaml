context:
  version: "0.14"

package:
  name: otagrum
  version: ${{ version }}

source:
  url: https://github.com/openturns/otagrum/archive/v${{ version }}.tar.gz
  sha256: f05655545d94f01c602e3c9f2077452d327c2880253eaffe5594ca146ee33292

build:
  number: 0
  python:
    version_independent: true

requirements:
  build:
    - cmake
    - swig
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - if: unix
      then: make
      else: ninja
    - if: win
      then: m2-diffutils
    - if: build_platform != target_platform
      then:
        - python-abi3 ${{ python_min }}.*
        - python ${{ python_min }}.*
        - cross-python_${{ target_platform }}
  host:
    - python-abi3 ${{ python_min }}.*
    - python ${{ python_min }}.*
    - openturns
    - pyagrum
  run:
    - python >=${{ python_min }}
  run_exports:
    - ${{ pin_subpackage('otagrum', upper_bound='x.x') }}

tests:
  - python:
      imports:
        - otagrum
      pip_check: false

about:
  homepage: http://openturns.org/
  license: LGPL-3.0-or-later
  license_family: LGPL
  license_file: COPYING.LESSER
  summary: OT/aGrUM module
  documentation: https://openturns.github.io/otagrum/master/
  repository: https://github.com/openturns/otagrum

extra:
  recipe-maintainers:
    - jschueller
